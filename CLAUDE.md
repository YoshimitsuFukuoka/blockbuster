# CLAUDE.md

Guidance for Claude Code (and other AI assistants) working in this repository.

## Project status

This repository is currently a blank slate: it contains only a `README.md`
(with the single line `# blockbuster`). There is no source code, build
configuration, dependency manifest, test suite, or directory structure yet.

Because of this, there are no established conventions, frameworks, or
workflows to follow. **Do not assume any particular language, framework, or
architecture** — confirm with the user before scaffolding a new project.

## Working in this repo right now

- Before adding code, ask the user (if not already specified) what the
  project's purpose, language, and stack should be.
- When the project is scaffolded, update this file with:
  - A real project overview (what `blockbuster` does).
  - Directory/module structure and where key logic lives.
  - Build, test, and lint commands (e.g. `npm test`, `pytest`, `make build`).
  - Code style and architectural conventions specific to this codebase.
  - Any non-obvious setup steps (env vars, services, local dependencies).

## Keeping this file current

This file should evolve alongside the codebase. Once meaningful structure
exists, replace this placeholder content with concrete, accurate
documentation rather than appending to it.
